apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: kickstarter-prediction-app
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
spec:
  rules:
    - host: kickstarter-prediction.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: kickstarter-prediction-app
                port:
                  number: 80